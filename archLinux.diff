diff --git a/electron-builder.yml b/electron-builder.yml
--- a/electron-builder.yml
+++ b/electron-builder.yml
@@ -47,6 +47,8 @@ dmg:
 # Linux 配置
 linux:
     target:
+        - { target: pacman, arch: x64 }
+        - { target: pacman, arch: arm64 }
         - { target: AppImage, arch: x64 }
         - { target: AppImage, arch: arm64 }
         - { target: deb, arch: x64 }

